build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - alembic upgrade head
    - python3 scripts/auto_seed_prompts.py
run:
  web: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
